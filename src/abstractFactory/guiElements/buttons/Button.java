package abstractFactory.guiElements.buttons;

/**
 * Паттерн предполагает, что есть несколько семейств продуктов,
 * находящихся в отдельных иерархиях классов (Button/Checkbox). Продукты одного
 * семейства должны иметь общий интерфейс.
 *
 * Это - общий интерфейс для семейства продуктов кнопки
 */
public interface Button {
    void paint();
}
